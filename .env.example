# CookPilot — Environment Variables
# Copy to .env.local and fill in values, or run `vercel env pull .env.local`

# Neon PostgreSQL (via Vercel Marketplace)
DATABASE_URL=

# AI Gateway (auto-provisioned via `vercel env pull`)
# VERCEL_OIDC_TOKEN=
# AI_GATEWAY_API_KEY=
